#include "..\..\core\script_macros.hpp"

private _version = 0.1;

["Extract", "When called, the module will spawn an AI controlled vehicle and will wait until all players are mounted before extracting.", "Starfox64 &amp; StatusRed", _version] call FUNC(RegisterModule);
