class VehBoostParams {
    class Car {
        maxSpeedInWater = -1;
        maxSpeed = -1;
    };
    class Ship {
        maxSpeedInWater = -1;
        maxSpeed = -1;
    };
    class Tank {
        maxSpeedInWater = -1;
        maxSpeed = -1;
    };
    class vn_armor_m113_base {
        maxSpeedInWater = 25;
        maxSpeed = -1;
    };
    // vehicle class (it works with inheritance, so all M151 jeeps will inherit this config from the m151_base class)
    class vn_wheeled_m151_base {
        maxSpeedInWater = -1;
        maxSpeed = -1;
    };
};
